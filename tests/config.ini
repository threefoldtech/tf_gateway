[main]
deploy=False                # deploy containers before start tests
destroy=False               # destroy containers after finishing tests
jwt=                        # needed if deploy or destroy is True

[tf_gateway]
node_ip=                    # Should be public ip
redis_port=
